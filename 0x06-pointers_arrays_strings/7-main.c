#include "main.h"
#include <stdio.h>

/**
 * main - check the code for
 *
 * Return: Always 0.
 */
int main(void)
{
char s[] = "Expect best. Prepare for worst. Capitalize on what comes.\n";
char *p;
p = leet;
printf("%s", p);
printf("%s", s);
return (0);
}
