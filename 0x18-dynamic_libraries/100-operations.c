/**
 * basic c operations (arithmetics)
 * hans254
 */
int add(int c, int d)
{
    return (c + d);
}
int sub(int c, int d)
{
    return (c - d);
}
int mul(int c, int d)
{
    return (c * d);
}
int div(int c, int d)
{
    return (c / d);
}
int mod(int c, int d)
{
    return (c % d);
}
